[test]
arch = x86_64, aarch64
os = linux
safety = free

cmd_begin
echo "succes"
sudo echo "sudo test"
echo "{VERSION}"
sudo echo "test"
cmd_end

remove_begin
echo "remove"
sudo echo "sudo remove"
remove_end

[test]
arch = x86_64, aarch64
os = termux
safety = free

cmd_begin
echo "succes"
echo "succes2"
cmd_end

remove_begin
echo "remove"
remove_end

[archlinuxcn]
desc = Setup archlinuxcn repository
arch = x86_64, aarch64
os = arch
deps = curl
sudo = true
safety = free

cmd_begin
  {BASH_RUN} archlinuxcn/archlinuxcn-install.sh
cmd_end

remove_begin
  {BASH_RUN} archlinuxcn/archlinuxcn-remove.sh
remove_end

[chaotic-aur]
desc = Setup chaotic-aur repository
arch = x86_64, aarch64
os = arch
deps = curl
sudo = true
safety = free

cmd_begin
  {BASH_RUN} chaotic-aur/chaotic-aur-install.sh
cmd_end

remove_begin
  {BASH_RUN} chaotic-aur/chaotic-aur-remove.sh
remove_end

[alps]
desc       = unified package manager frontend
author     = adrianpriza-ai
version    = 1.0.3
arch       = x86_64, aarch64, armv7l
os         = linux, termux
sudo       = true
safety     = free

cmd_begin
  {BASH_RUN} https://alps-project.pages.dev/install.sh
cmd_end
upgrade_begin
  {BASH_RUN} https://alps-project.pages.dev/install.sh
upgrade_end
remove_begin
  {BASH_RUN} https://alps-project.pages.dev/uninstall.sh
remove_end
purge_begin
  {BASH_RUN} https://alps-project.pages.dev/uninstall.sh --purge
purge_end
